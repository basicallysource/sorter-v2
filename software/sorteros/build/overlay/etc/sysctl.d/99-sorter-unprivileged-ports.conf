# Let non-root processes bind ports 80+ so sorter-ui-dev.service (running as
# unprivileged user) can serve the UI on port 80 — gives users a plain
# http://<host>.local URL without a port suffix.
net.ipv4.ip_unprivileged_port_start = 80
