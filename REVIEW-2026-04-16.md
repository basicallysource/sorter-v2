# LegoSorter v2 — Cross-Project Review & Tracking Doc

**Created:** 2026-04-16 · **Scope:** full repo (sorter backend/frontend, Hive backend/frontend, firmware, docs, systemd, install)

This file is a living tracker. Mark items `[x]` as they land. Expand or split items as needed. Review produced by multi-expert audit (Architect / Security / UX / Frontend / SRE / Tech-writer / PM perspectives) with claims cross-verified against the actual code — false alarms caught and noted in §1.

---

## 0. Status — What's Already Done

- [x] **P0 #4 — Git LFS pre-flight in installer** (commit `00c7aba`).
- [x] **P1 #9 — Split 1342-line `profiles/+page.svelte`** (commit `c0efb29`). Orchestrator now 613 lines; extracted `ProfileCard`, `ProfileApplyModal`, `ProfileDetailsModal`, `ProfilePagination` + `types.ts` / `api.ts` / `format.ts`.
- [x] **Follow-up — Dedup `formatRelativeTime`** (commit `361377d`). `SortingProfileDropdown.svelte` now imports canonical helper.
- [x] **P0 #3 — Tighten Pico udev rule** (commit `c1952e2`). `0666` → `0660` + `plugdev`; installer auto-adds user; docs updated.
- [x] **P1 #7 — Run Hive containers as non-root** (commit `94d315d`). `app` user in backend, built-in `node` in frontend.
- [x] **P1 #5 — Harden sorter systemd units** (commit `09e2549`). ProtectSystem/PrivateTmp/NoNewPrivileges/ProtectHome/RestrictAddressFamilies + StartLimit rate-limit + ReadWritePaths allowlist.
- [x] **P0 #1 — Lock down sorter API bind and CORS** (commit `46085c5`). Default `127.0.0.1`, `SORTER_API_HOST` env var to expose, CORS derived from bind.
- [x] **Cleanup — Remove stray backslash-path duplicates** (commit `562e283`). Dead Pre-Restructure-Artefakte aus `software\/sorter\/...` weg (749 Zeilen).
- [x] **P0 #2 — Encrypt API tokens at rest** (commit `4f3ea96`). Fernet per-install seed (`.secret_seed`, 0600), HKDF-SHA256 key derivation, transparent plaintext migration on first read.

**Branch state vs `main`:** `sorthive` is 31+ commits ahead of `main`, `main` has 0 commits not in `sorthive`. Merge base = `main` tip (`2a7f84b`). ⇒ `sorthive → main` is a fast-forward once ready.

---

## 1. Verified vs Unverified Agent Claims

Three parallel audit agents produced findings. Before acting on any, claims were spot-checked:

| Claim | Status |
|---|---|
| `profiles/+page.svelte` = 1342 lines | ✅ Verified (now 613 after split) |
| `AppHeader.svelte` = 609 lines (agent said 567) | ⚠️ Larger than reported |
| `vision_manager.py` = 2584 lines | ✅ God-object confirmed (bigger than agent said) |
| `local_state.py` = 1277 lines | ✅ Verified |
| `coordinator.py` = 106 lines "god object" | ❌ False — Coordinator is thin |
| Sorter API `CORS allow_origins=["*"]` | ✅ Verified (`server/api.py:51`) |
| Python 3.13 vs 3.14 mismatch | ❌ Pyproject says `>=3.13` — no evidence of 3.14 build |
| `.env` leaked in git history | ❌ **FALSE** — `software/hive/.gitignore` excludes `.env` + `backend/.env`; `git ls-files` clean |
| systemd hardening missing | Not re-verified, likely true |
| Large Svelte components mix concerns | ✅ Verified by line counts |
| Rounded corners / left borders forbidden | ✅ Matches saved design rules |

**Takeaway:** Treat agent findings as leads, not facts. Leaked-secrets claim would have been an embarrassing false alarm.

---

## 2. Executive Summary

**Product shape:** Two SvelteKit frontends (sorter local UI + Hive community), two Python/FastAPI backends, Raspberry Pi Pico firmware (2 role variants), Jekyll docs. Solo-ish maintainer scope.

**Overall health:** Working system with real hardware integration and a thoughtful SortHive split. **Not production-hardened.** Main risks:

1. **Local backend security** — sorter API fully open (CORS `*`, no auth). Defensible IF strictly LAN-only monitoring; serious the moment it's port-forwarded or a Pi is stolen.
2. **Frontend maintainability wall** — megacomponents, two diverging UI component libraries. Debt compounding.
3. **Ops immaturity** — no CI, fragile install.sh failure modes, unhardened systemd units, no observability, docs drift.

**Strategic question:** Personal lab + showcase, or shipped-to-other-makers? Current state is halfway. Answer determines how much of below is worth doing.

---

## 3. Top 15 Findings — Tracker

### P0 — Fix before any external user runs this

- [x] **1. Sorter API has no auth + CORS `*`** — bind lockdown done in `46085c5`. CORS narrowed to derived origins. **Still open:** bearer-token auth (anyone who *is* allowed to hit the API can still hit every endpoint unauthenticated). Revisit if exposing on LAN regularly.

- [x] **2. Plaintext API keys in SQLite blob** — fixed in `4f3ea96`. Fernet-encrypted with per-install `.secret_seed`; transparent migration of legacy plaintext entries.

- [x] **3. Udev rule world-writable Pico access** — fixed in `c1952e2`.

- [x] **4. Git LFS silent failure during install** — fixed in `00c7aba`.

### P1 — Ship-quality hygiene

- [x] **5. systemd hardening** — done in `09e2549`. Backend/UI units sandboxed with explicit `ReadWritePaths`. `After=` was already present on the UI unit.

- [ ] **6. No CI pipeline** (`.github/workflows/` empty)
  - Proposed minimal: `ruff`, `pyright`, `pnpm build`, `test_install_in_docker.sh`, `systemd-analyze verify`.
  - *Decision deferred — Marc explicitly said CI is later.*

- [x] **7. Docker images run as root** — fixed in `94d315d`.

- [ ] **8. VisionManager is a genuine god-object** (`software/sorter/backend/vision/vision_manager.py` — 2584 lines)
  - Camera capture + region detection + heatmap diff + Mog2 + OpenRouter + classification + carousel + aux sample collection + cache invalidation.
  - *Mitigation:* carve out `CameraFramework` (capture), `DetectionPipeline` (inference), `SampleCollector` (aux). Incremental, no big-bang rewrite.

- [x] **9. `profiles/+page.svelte` at 1342 lines** — split in `c0efb29`.

- [ ] **10. Two UI component libraries diverged** (`sorter/frontend/src/lib/components/*` vs `hive/frontend/src/lib/components/*`)
  - `Modal`, `Badge`, `Spinner` exist on both sides with incompatible APIs.
  - *Mitigation:* extract `software/ui/` pnpm workspace package. Both frontends consume it.

### P2 — Maintainability and correctness

- [ ] **11. Threading safety audit on sorter backend**
  - `shared_state` mutated from FastAPI threadpool + main loop without explicit locks on some dicts (`camera_device_preview_overrides` flagged).
  - *Mitigation:* document thread ownership per class; add `threading.RLock` with timeout at mutation points.

- [ ] **12. No structured logging / observability on deployed machines**
  - `print()`-style logging, no metrics endpoint, no error aggregation, no log rotation.
  - *Mitigation:* `python-json-logger` in backend; `/api/metrics` Prometheus endpoint (reuse existing `runtime_stats`); document `journalctl --vacuum-size=500M`.

- [ ] **13. Docs drift** (`docs/sorter/installation.md`, `docs/getting-started.md`, `docs/hive/index.md`)
  - Wizard walkthrough "still being written"; Hive page stubbed but `docker-compose.prod.yml` real; troubleshooting references logout/login the udev rule eliminates.
  - *Mitigation:* docs sprint; pre-commit hook that bumps `last_verified` when referenced files change.

- [ ] **14. Design system only half-committed** (sorter frontend)
  - `layout.css` defines tokens but 50+ hardcoded `#D01012`/`#00852B` in components; no `<Button>`/`<Card>`/`<Alert>` primitives; dark-mode partial.
  - *Mitigation:* pair with #10 — build shared primitives once.

- [ ] **15. Test coverage thin on critical paths** (~21% lines, agent estimate, unverified)
  - No tests on: state machine transitions, vision pipeline, API endpoints, Hive-sorter end-to-end.
  - *Mitigation:* not "raise coverage %" — add focused tests for sample upload retry + sorting-profile load/version compat.

### Follow-ups surfaced during recent work

- [x] **Dedup `formatRelativeTime`** — done in `361377d`.
- [ ] **Split `AppHeader.svelte`** (609 lines) — next logical mega-component after `profiles/+page.svelte`.

---

## 4. Strategic Observations (PM-level)

- **Scope vs throughput.** Mechanical + electronics + firmware + vision ML + two web apps + docs. One maintainer cannot harden all of it. Pick:
  - **(A) Personal lab + open-source showcase.** Accept rough edges. Focus on hardware + ML quality. Mark ops/security "LAN-only, single user".
  - **(B) Ship to other makers.** Commit to CI, install reliability, security baseline, docs accuracy. Cut features if needed.
  - Codebase today is halfway. Pick and name it.

- **Sorter ↔ Hive split is correctly drawn** but the **contract layer is missing**. Both sides invent their own payload schemas (`server/sample_payloads.py` on sorter, `app/services/sample_payloads.py` on hive). One shared JSON Schema → codegen both sides. Prevents the silent-breakage class of bug this split is most vulnerable to.

- **Two frontends without a shared package is a regret in waiting.** Every week they diverge more. Extract `software/ui/` before Hive grows.

- **"Simplify profile lifecycle" memory vs 1342-line profiles page** — direct contradiction between stated direction and current reality. (Partially addressed by split in `c0efb29`, but lifecycle simplification still pending.)

- **Feature gaps (what users expect, missing today):**
  - Profile version diff UI ("what changed between v1 and v2")
  - Bulk sample review (approve/reject N at once)
  - Machine config export/import (for hardware swap)
  - Real-time metrics dashboard (sort rate, accuracy trend)
  - Undo on profile activation

- **Hardware-aware UX gaps:**
  - Hardware error banner has multiple entry points with unclear difference ("Refresh Status" vs "Retry Home")
  - ArUco calibration has no user-facing route — if cameras move, users stuck
  - Camera setup bifurcated between setup wizard and settings page

---

## 5. Proposed Roadmap (if pursuing option B)

| Phase | Focus | Deliverables |
|---|---|---|
| **Week 1 — Safety net** | P0 items | ~~LFS pre-check~~ ✅ · CORS lockdown / localhost bind · encrypted secrets · udev rule fix |
| **Week 2 — CI + systemd hardening** | P1 #5, #6, #7 | `.github/workflows/test.yml` · hardened unit files · Docker `USER` directives |
| **Week 3 — Shared UI package** | P1 #10 | `software/ui/` pnpm workspace with Modal/Badge/Button/Card/Alert · both frontends migrated |
| **Week 4 — Profile page refactor** | P1 #9 continuation | ~~Component split~~ ✅ · Edit+Save lifecycle per memory directive |
| **Week 5 — Schema contract** | Strategic | Shared JSON Schema for sample payloads · codegen pydantic models both sides |
| **Week 6 — VisionManager carve-out** | P1 #8 | Extract `CameraFramework` first (lowest risk), then `DetectionPipeline` |
| **Ongoing** | P2 #11–15 | Tests, logging, docs, design tokens — per PR |

Skip this roadmap entirely if option (A) — only Week 1 P0 still matters.

---

## 6. Things That Are Good (Don't Break These)

- **SortHive architectural split** — well-reasoned, clear planning.
- **`.gitignore` hygiene** — `.env` files properly excluded. (False alarm caught.)
- **uv for Python deps** — right choice, `dev.sh` convenient.
- **Supervisor process pattern** — separating hardware loop from API is correct.
- **Sharp-edged sorter design language** — intentional and consistent where enforced.
- **Makefiles + dev.sh helpers** — meaningful onboarding friction reduction.
- **Git LFS for models** — right tool; pre-check now in place.

---

## 7. Open Questions (to scope any follow-up work)

1. **Ship target** — lab-personal (A) or maker-distributable (B)?
2. **Sorter deployment model** — strictly LAN / single user, or occasionally port-forwarded / remote?
3. **Hive rollout** — concept phase, or running somewhere?
4. **Bandwidth** — solo, or is anyone else touching the code?
5. **Priority bias** — if forced to pick ONE of (security / refactor debt / UX polish / docs accuracy / CI) for next 2 weeks, which?

---

*Maintenance: when an item is completed, leave it checked with commit sha in parentheses. When a finding is reclassified (e.g. reduced scope, or superseded), strike through rather than delete — preserves audit trail.*
