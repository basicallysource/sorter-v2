# Detector Runtime Status

The canonical version of this document now lives in:

- `../../Documentation/runtime-status.md`

This file is intentionally kept as a short pointer so the software tree still has a discoverable entry when people look for detector benchmarking and deployment guidance from inside `software/client/`.
