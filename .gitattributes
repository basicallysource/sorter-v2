software/sorter/backend/bundled_models/**/*.onnx filter=lfs diff=lfs merge=lfs -text
software/sorter/backend/bundled_models/**/*.bin filter=lfs diff=lfs merge=lfs -text

# Docs images are NOT in git at all: they live in the asset service, served at
# https://assets.basically.website. Upload with docs/scripts/upload_image.py.
# Do not add image LFS rules for docs — Vercel does not materialize Git LFS
# objects on deploy.
