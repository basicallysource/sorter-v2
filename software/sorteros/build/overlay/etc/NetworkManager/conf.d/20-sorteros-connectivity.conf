# Online means the internet answers, not that there's a route. NetworkManager
# asks this address through each network and moves traffic off one that
# doesn't answer (a cable into a switch with no internet), and sorteros-network
# opens the setup network when none does.
[connectivity]
uri=http://connectivity-check.ubuntu.com./
interval=60
