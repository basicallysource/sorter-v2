# Device Detector Benchmarking

The canonical version of this document now lives in:

- `../../docs/device-benchmarking.md`

This file is intentionally kept as a short pointer so the software tree still has a discoverable entry when people look for the repeatable benchmark workflow from inside `software/sorter/backend/`.
