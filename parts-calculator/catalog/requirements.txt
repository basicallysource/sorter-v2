# Python deps for slicer/filament.py + scripts/sync_bucket.py.
# Pinned to match Spencer's machine so CI renders/output stay comparable.
numpy==2.2.6
matplotlib==3.11.1
trimesh==4.8.3
# the uid stamp (catalog/engrave.py): face outlines and the text, and the
# boolean that recesses it -- the same versions everywhere or the stamped
# bytes, and so their content-addressed URLs, drift between machines
shapely==2.1.2
manifold3d==3.5.2
scipy==1.17.1
boto3==1.42.44
