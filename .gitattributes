software/sorter/backend/bundled_models/**/*.onnx filter=lfs diff=lfs merge=lfs -text
software/sorter/backend/bundled_models/**/*.bin filter=lfs diff=lfs merge=lfs -text

# Docs images are NOT in git at all: originals and web-optimized versions live
# in the basically-docs DO Spaces bucket, served at https://img.basically.website.
# Upload with docs/scripts/upload_image.py. Do not add image LFS rules for docs —
# Vercel does not materialize Git LFS objects on deploy.
