# Model Artifacts

The canonical version of this document now lives in:

- `../../Documentation/model-artifacts.md`

This file is intentionally kept as a short pointer so the software tree still has a discoverable entry when people look for detector exports, compiled formats, and target mappings from inside `software/client/`.
