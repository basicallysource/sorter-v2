[project]
name = "sorteros-build-dashboard"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "tomli>=2.0; python_version < '3.11'",
]

[tool.uv]
dev-dependencies = []
