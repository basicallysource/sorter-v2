[project]
name = "sorteros-portal"
version = "0.1.0"
description = "SorterOS captive-portal backend (Wi-Fi onboarding)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.32",
    "pydantic>=2.9",
    "cryptography>=43",
]

[tool.uv]
package = false
