ColorChecker source assets used for camera calibration references.

- `color-checker.io`: BrickLink Studio project file
- `color-checker.ldr`: LDraw export of the same model

These are source/design files, not runtime assets.
