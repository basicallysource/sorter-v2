.claude/
# Machine-local agent notes (private: machine names, tailnet, access details).
# The committed AGENTS.md files are public.
AGENTS.local.md
.tmp-*

machine.toml

software/sorteros/build/.env
software/sorteros/build/*.authkey
software/sorteros/build/out/
software/sorteros/build/cache/
software/sorteros/v3/build/.env

# Local debug captures and ad-hoc review artifacts
/c-channel-3-zone-editor.png
/c2-debug.png
/classification-*.png
/classification-*.md
/.playwright-mcp/
/detail-requests.txt
/samples-nav-requests.txt
/sample-detail-blank.png
/software/sorter/backend/calibration_image_sample_data_20x128x128x3_float32.npy
/docs/node_modules/
/docs/.svelte-kit/
/docs/build/

# Rendered WireViz output. build-harness.sh generates it from the YAML sources
# beside it; upload-harness.py publishes it to the asset service and prints
# the URLs the docs reference. Never committed: no images go into git history.
/electronics/wire_harness/out/
/data/
/test.db
/test-profile-parts.db

# Postgres snapshots taken before deploys/migrations — local only, often large
/backups/

.DS_Store

docs/scripts/__pycache__/
